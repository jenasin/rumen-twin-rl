# RumenTwin-RL — pinned to the versions the reported results were produced with.
#
# Optional external tools, needed only for the conference deliverables in paper/:
#   pandoc    -> RumenTwin-RL_manuscript.docx and .pdf   (brew install pandoc)
#   tectonic  -> the PDF only; xelatex/pdflatex also work (brew install tectonic)
# Without them the pipeline still produces paper/paper_draft.md and all figures.
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
PyYAML>=6.0
gymnasium>=1.0,<2.0
stable-baselines3>=2.4,<3.0
torch>=2.0
pyarrow>=12.0
tqdm>=4.65
pytest>=7.0
streamlit>=1.49
python-docx>=1.0      # A4 page setup for the generated Word file
